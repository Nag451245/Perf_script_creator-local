# Copy to .env (gitignored). Alternative to perfscript.config.json for secrets.
# Path to the PerfScript repo's "backend" folder (the reused engine).
PERFSCRIPT_ENGINE=D:/Users/nagendra.bpuchala/Documents/jmeter-script-creator-main/jmeter-script-creator-main/backend
# JMeter install (only needed for --run).
JMETER_HOME=D:/apache-jmeter-5.6.3
# Gemini key for Phase 4 escalation (optional).
GOOGLE_API_KEY=
